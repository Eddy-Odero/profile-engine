"""
github.py  (Phase 4 - not yet implemented)

Will fetch live data via the GitHub API (requests + a GITHUB_TOKEN
env var for higher rate limits):
    - repository count, stars, followers
    - contribution count / recent activity
    - languages used
    - pinned repositories

Should return a dict shaped like MOCK_GITHUB_STATS in build.py so it
can be dropped in as a straight replacement:

    from github import fetch_github_stats
    stats = fetch_github_stats(username="EddyOdero")
"""

raise NotImplementedError("github.py is scaffolded for Phase 4, not implemented yet.")
